#include <stdio.h> //preprocessor directive

int main(){

    int age = 25;
    int year = 2025;
    int quantity = 1;

    printf("You are %d years old\n", age); // %d is a format specifier %d is or decimal
    printf("The year is %d\n", year);
    printf("You have ordered %d x items\n", quantity);

    return 0; // commonly at end of main function main function typically ends in integer 0 means successful runs
}